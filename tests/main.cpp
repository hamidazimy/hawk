// doctest test-runner entry point.
//
// DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN must be defined in exactly one translation
// unit. It pulls in the framework implementation and provides main(), so every
// other test file only needs to include <doctest/doctest.h> and register cases.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
